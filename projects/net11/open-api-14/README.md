# Binary file responses in OpenAPI

File result types (`FileContentResult`, `FileContentHttpResult`, `FileStreamResult`, `FileStreamHttpResult`) now map to `type: string, format: binary` in generated OpenAPI documents.

