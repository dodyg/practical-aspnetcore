# Blazor Label and DisplayName components

 `Label` component now renders accessible labels (nested and `for`/`id` patterns) and a `DisplayName` component that shows a property's
display name from `[Display]`/`[DisplayName]` attributes 
