# QuickGrid in static SSR

 `QuickGrid` sorting and pagination now work in statically rendered Blazor SSR pages. Sortable headers and the paginator render as enhanced forms that update URL query-string state, so users can sort, page, refresh, and share
links without an interactive circuit or WebAssembly runtime.
