# Special types binding

These are special types that the framework supports binding without any explicit attributes:

- `HttpContext` 
- `HttpRequest` 
- `HttpResponse` 
- `CancellationToken` 
- `ClaimsPrincipal` - From (HttpContext.User).
