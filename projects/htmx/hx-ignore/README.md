# hx-ignore and hx-disable

`hx-ignore` prevents htmx from processing an element and its subtree. In htmx 4, `hx-disable` has the former `hx-disabled-elt` behavior: it disables an element while its request is in progress.
