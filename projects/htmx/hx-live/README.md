# hx-live

The `hx-live` extension adds DOM-based reactive scripting, `data` state, `:text` bindings, and helpers such as `q`, `debounce`, `nextFrame`, and `take` without a separate framework.

The progress bar demonstrates the `hx-live` attribute itself: its value is recomputed whenever the shared `data.count` state changes.
