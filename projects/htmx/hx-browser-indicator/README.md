# hx-browser-indicator

The `hx-browser-indicator` extension displays the browser tab's native loading indicator while htmx requests are active. The sample also shows an inline fallback indicator for browsers that do not support the Navigation API.

