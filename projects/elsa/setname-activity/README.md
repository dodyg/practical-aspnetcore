# SetName Activity

This sample demonstrates the `SetName` activity. The `SetName` activity sets the name of the workflow instance, which can later be read back from `WorkflowExecutionContext.Name`.




